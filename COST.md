# COST.md — 成本模型同被濫用分析

> **前提：免費公開專案，唔收錢。所以設計約束係「成本必須貼近零」，
> 而且最重要嘅係「唔可以有『被人 call 爆然後收錢』嘅路徑」。**
>
> 下面唔係估算，係查實嘅免費層額度 ＋ 逐個服務嘅故障模式。

---

## 0. 答案先行

**現階段：US$0／月。而且設計上冇任何計費型憑證 → 冇「被人打爆然後收錢」呢條路。**

三個關鍵事實：

1. **公開專案唯一嘅 runtime 成本係 Cloudflare Worker**，而免費層**超額係「停」，唔係「收錢」**
2. **我哋冇任何 metered key。** Google Maps 就係因為「多人用就燒 key」而被剔除 —— 呢個決定嘅真正價值唔係慳錢，係**消除咗唯一一個可以產生帳單嘅嘢**
3. **LLM 唔喺運行路徑。** 全部收集器都係純 script（HTTP fetch），零 token

---

## 1. 免費層額度（2026 查實）

| 服務 | 免費額度 | 超額會點 |
|---|---|---|
| **Cloudflare Pages**（靜態網站） | **請求同流量無限**，500 build/月 | 唔會超（靜態檔） |
| **Cloudflare Workers**（proxy ＋ cron） | **100,000 請求／日**，每次 10ms CPU | **停到第二日**，唔收錢 |
| **Cloudflare R2**（大檔倉庫） | 10 GB 儲存、100 萬寫入/月、**egress 免費** | 停寫入，唔收錢 |
| **Cloudflare D1 / KV** | 有免費層（D1 近期收緊） | 停，唔收錢 |
| **LandsD tile API** | 冇公開數字，但**唔係我哋嘅伺服器** | **唔係我哋俾錢** |
| **Esri World Imagery** | 公開唯讀、免 key | 同上 |
| **LLM（DeepSeek）** | — | **冇公開入口可以觸發** |

**冇一個要 credit card。** 呢個係重點：冇卡 = 冇得自動扣錢。

---

## 2. 「被人 call 爆」逐個分析

| 服務 | 邊個可以打？ | 打爆嘅後果 | 護欄 |
|---|---|---|---|
| **Pages（靜態）** | 全世界 | **冇成本**（無限流量） | 唔需要 |
| **Worker（proxy）** | 全世界 | 10 萬 req/日 → **停一日，唔收錢** | rate limit ＋ 邊緣 cache |
| **LandsD tile** | 只經 Worker | **佢哋嘅頻寬，唔係我哋嘅錢**；但**可能封我哋** | **cache tile**（見下） |
| **3D Tiles（有 key）** | 只經 Worker | key 唔會洩漏；Worker 被濫用 → LandsD 封 key | rate limit ＋ 白名單 |
| **LLM** | **冇人可以觸發** | — | 唔喺運行路徑 |
| **VPS** | 唔公開 | — | `SECURITY.md` 已禁止喺 repo 出現 IP |

### 唯一真正嘅風險：唔係錢，係「被封」

LandsD 嘅服務條款寫明「**唔可以短時間內大量請求**」。
如果有人透過我哋個 Worker 猛打 tile：

- **唔會產生帳單**
- 但會被 LandsD **封鎖**（可能連 IP 或連 key）
- 結果 = **個站壞**，唔係 **個銀包痛**

**所以 tile cache 係安全措施，唔係效能優化。** 呢個係 `PRIMITIVES.md` 已經寫入嘅設計要求。

---

## 3. LLM token：設計上係 0

呢點要講得好清楚，因為佢係最大嘅潛在經常性成本。

| 功能 | 用唔用 LLM？ |
|---|---|
| 171 個數據源收集 | ❌ 純 HTTP fetch |
| 觸發引擎（開颱風模式） | ❌ **規則式**（生死資訊要可稽核，唔准 LLM） |
| 地理編碼（文字地名 → 座標） | ❌ **gazetteer ＋ ALS 官方查詢**（唔准 LLM 出座標，會幻覺） |
| 過期偵測（freshness） | ❌ 純比較時間戳 |
| 地圖 tile | ❌ 直接攞圖 |
| **事件層（新聞 → 地圖）** | 🟡 **唯一可能用 LLM 嘅地方** |

### 事件層：連呢個都可以做到 0

原本設計係「RSS → LLM 抽地名時間 → 地圖」。但其實：

- **標題、日期、連結** = RSS 本身已經有，唔使抽
- **地名** = gazetteer 字串匹配（我哋已經有 ALS 官方查詢 ＋ 地名表）
- **係唔係值得落圖** = 關鍵詞規則

**所以 v1 事件層都可以零 token。** LLM 係之後想提升準確度先加嘅**可選升級**，
而到時亦必須跟 Cyrus 定嘅硬規則：

> **高頻（≤1 小時）必須係純 script，零 token。LLM 只可以低頻（日／週）＋ 細 context。**

如果將來真係加 LLM 做新聞分類：**每日 2 次**（唔係每小時），每次 ~30 條新項目 ≈ 7k tokens
→ 約 42 萬 tokens／月。以現行 DeepSeek 價格係**每月唔夠 US$1**。
（⚠️ 具體單價我未查實，所以唔俾你一個實數 —— 要就我去查。）

---

## 4. 硬規則（寫入 spec，唔准違反）

1. **永遠唔加 metered API key。** 任何「用得多就收錢」嘅服務一律唔准接入。
   Google Maps 就係第一個因此被剔除嘅。
2. **LLM 唔准入運行路徑。** 只可以低頻批次，而且要事先報「幾頻密 ＋ context 幾大」。
3. **Worker 一定要有：目標 URL 白名單 ＋ rate limit ＋ 邊緣 cache。**
   白名單防止被當免費跳板；cache 防止我哋被封。
4. **唔准用 POST 通用代理。** 只做已知 GET，否則 Worker 會變成開放 proxy。
5. **任何新服務接入前，先答三條**：
   - 免費層幾多？超額會**停**定**收錢**？
   - 要唔要 credit card？
   - 有冇「用量越大越貴」嘅機制？

**答唔到第 3 條（即係有計費）→ 唔准接入。**

---

## 5. 咁樣 Cyrus 實際要俾咩錢？

| 項目 | 成本 |
|---|---|
| 托管（Pages ＋ Worker） | **US$0** |
| 數據（171 個源） | **US$0**（全部免 key） |
| 地圖 tile | **US$0** |
| 3D 模型 | **US$0**（地政總署免費授權，只需標註） |
| LLM | **US$0**（唔喺運行路徑） |
| 自訂域名（可選） | 約 US$10／年 —— **唔要就用 `*.pages.dev`，免費** |

### 唯一現實嘅將來成本

**如果個站真係紅到超過 10 萬 req／日：**

- 免費層會**停**
- 想繼續 → Cloudflare Workers Paid = **US$5／月**（包 1,000 萬 req）
- 呢個係**唯一**要諗嘅階梯，而且係你主動俾，唔係被扣

**冇任何情況會出現「瞓醒見到帳單」。**

---

## 6. 交貨前檢查

```bash
# 1. 有冇違反「唔准 metered key」？
grep -rEn 'AIza|googleapis.*key|maps.googleapis' --include='*.json' --include='*.md' --include='*.js' . | grep -v '唔准\|never\|ruled out'

# 2. Worker 有冇白名單同 rate limit？（睇 wrangler.toml / worker.js）
# 3. 有冇任何地方喺運行路徑叫 LLM？
grep -rEn 'openai|deepseek|anthropic|api.*completions' --include='*.js' --include='*.ts' src/ 2>/dev/null
```

第 3 條掃到嘢 = 要交代「幾頻密 ＋ context 幾大」先可以留。
