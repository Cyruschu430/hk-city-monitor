# HANDOVER.md — handing HK City Monitor to another agent

Written 2026-09-23. Two agents have worked this repo. This is how to move between
them without either one redoing the other's work or re-learning its pitfalls.

## Where the state lives (read in this order)

1. **`AGENTS.md`** — start at `## Current state (as of 2026-09-23)`. The `## Run sequence`
   below it is **history**: it is how the app was built and it explains the architecture,
   but v0.2 is built. Do not re-run it.
2. **`AGENTS.md` → `## Pitfalls already paid for`** — 18 of them. Every one cost a
   measured wrong turn. Pitfall 16 and 18 are the expensive ones.
3. **`WORK_ORDER.md`** — what is left, with the current state of each item measured
   rather than assumed. §0.5 and §0.6 are the two steps only Cyrus can do.
4. **`PRODUCTION_CHECKLIST.md`**, **`SPEC_TRANSPORT_PANELS.md`**, **`SPEC_LAYOUT_AND_LIVE.md`** —
   the detail behind the work order.

## Handing over from DSH to the VPS-side agent

DSH's last commit was `e2fa015` (`docs: record D3/D4/D6 completion...`, touching only
`design/ROADMAP_v0.4.md`). **A series of commits has landed since**, made by the
VPS-side agent. The count is deliberately not written here — it was wrong the moment it
was typed (17, not the 16 written first) and goes stale on the next commit. Run
`git log --oneline e2fa015..HEAD` for the live list; it is reproduced below as of
2026-09-23:

```
beaab0e docs: the collector schedule and how it publishes
59ef6c8 feat(analytics): a baseline collector, the app reading its output, and one rule that consumes it
7768a73 docs(agents): pitfall 18 + drop two unreferenced fixtures
8fc8039 docs: the two steps only Cyrus can do
69460e1 fix(css): the HK market up/down colour never painted (specificity)
56fe211 feat(ui): hand-rolled SVG sparkline in the market table, and the fixture that hid it
c54a1c7 fix(ui): the status cell reports the system, not one source's trigger freshness
a20c4bf docs(agents): point at the remaining work order and specs
886bea5 docs: the remaining work order, the P0 checklist, and the layout/transport specs
990ba1d docs(agents): current state as built, the DOM verification method, and pitfalls 12-17
589ba7c feat(data): HOY TV 76 and RTHK live streams (16 -> 18)
988bf66 chore(legal): AGPL-3.0 LICENSE, generated ATTRIBUTION.md, .env.example, validator fix
2722462 feat(data): MTR next train + KMB arrivals panels, with the rmk column kept
a61c282 feat(ui): hide a panel and remember it, with a restore chip and the poll loop stopped
f2d44a5 feat(ui): Dark/Light/System theme with a pre-paint attribute and an OS listener
90e5d04 feat(ui): category tabs derived from the source group field, with a shareable ?tab= URL
db28fb7 feat(ui): layer state three-sync (map + ?layers= URL + localStorage) + missing rail icon
```

`git log --oneline e2fa015..HEAD` reproduces that list.

## How DSH runs, and what to start

```
npx @deepseek-ai/dsh web          # local web UI; already running as of 2026-09-23
```
- Sessions: `C:\Users\cyrus\.dsh\sessions\` (13 files; the newest is 4.6 MB from 09-23 08:43)
- Global instructions: `C:\Users\cyrus\.dsh\AGENTS.md` — it states that a **project-level
  AGENTS.md takes precedence**, so the repo's file wins.
- Default model: `opencode-go/deepseek-v4-flash-vision-exp`

**Start a NEW session rather than resuming the 4.6 MB one.** That session carries the
pre-work plan ("v0.2 is a rebuild") as live context; a new session reads the current
`AGENTS.md` instead, and the 16 commits above are already summarised in it.

## The paste-in brief for a new session

```
You are taking over C:\hk-city-monitor. Another agent has landed a series of commits
since your last one (e2fa015, a ROADMAP docs commit) — run
`git log --oneline e2fa015..HEAD` to see them. Do not re-do any of it.

Before you plan anything:
1. Read AGENTS.md, starting at "## Current state (as of 2026-09-23)". The "Run
   sequence" below it is HISTORY — the app is built. Do not re-run it.
2. Read AGENTS.md → "## Pitfalls already paid for" (18 of them). Pitfall 16 and 18
   are the expensive ones.
3. Read WORK_ORDER.md for what is left. §0.5 and §0.6 are Cyrus's steps, not yours.

How to verify a UI change (Pitfall 16): NOT by looking at a screenshot. A screenshot
review called a two-column grid single-column and called a fixed build broken. Drive
the page over CDP and read window.__hkcm / window.__map / window.__cameras.

Commands that work from C:\hk-city-monitor\web:
  npx tsc --noEmit
  npm test
  set VITE_WORKER_BASE=http://localhost:8787&& npm run build    (build WITHOUT this
    ships a front end that cannot reach the Worker: 7 panels go to error state)

Do NOT: fill the 10 blank licence fields (a wrong licence is worse than a missing
one); hand-edit ATTRIBUTION.md (it is generated by scripts/build_attribution.py);
commit data/live_streams.json.bak-*; or add a 9th render kind to render.ts.

Blocked on Cyrus, not on you: `npx wrangler login` (59% of sources need the Worker),
the aisstream.io key, and the hourly collector schedule in WORK_ORDER §0.6.
```

## Handing back the other way

The VPS-side agent reads a **read-only mirror** at `/home/admin/hkcm-src/`. It goes
stale after every patch, and auditing against it reports the pre-patch code — re-copy
`web/src` and `AGENTS.md` before auditing. Edits always land on the PC
(`C:\hk-city-monitor`), never in the mirror.
