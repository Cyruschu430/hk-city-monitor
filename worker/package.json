{
  "name": "hk-city-monitor-worker",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "CORS proxy + edge cache + 3D URL injection for HK City Monitor. The project's only server-side piece.",
  "scripts": {
    "dev": "wrangler dev",
    "check": "node --check src/index.js"
  },
  "devDependencies": {
    "wrangler": "^4.38.0"
  }
}
